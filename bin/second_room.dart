import 'room.dart';

class SecondRoom extends Room{
 SecondRoom({List<String> roomItems, String name, String description}) : super(roomItems: ['key'],doorColor: 'Blue door', description: description);

}